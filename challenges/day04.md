## 📅 Day 04: Linux 권한 관리 및 인프라 자동화 스크립트 배포

### 1. Task

- **요구사항**: xFusionCorp Industries의 백업 자동화를 위한 쉘 스크립트 배포 프로세스
- **대상 인프라**: App Server 2 (`stapp02`)
- **목표**: 배포된 `/tmp/xfusioncorp.sh` 스크립트에 대해 전체 사용자 실행 권한을 부여하여 자동화 작업이 원활히 수행되도록 설정

### 2. 해결 과정 (Troubleshooting & Action)

#### 2-1. 파일 상태 확인 (Initial Audit)

`/tmp` 디렉토리에 위치한 스크립트의 초기 권한 상태를 확인하여 현재 실행이 불가능한 상태(`-rw-r--r--`)임을 진단.

```bash
ls -l /tmp/xfusioncorp.sh
```

#### 2-2. 실행 권한 부여 (chmod)

해당 스크립트가 시스템 백업을 위해 모든 유저에 의해 트리거될 수 있도록 8진수(Octal) 모드를 활용하여 실행 권한을 부여함. 보안과 기능성을 모두 고려하여 `755` (rwxr-xr-x) 권한을 설정함.

```bash
# 일반 유저 권한으로는 수정 불가하므로 sudo 활용
sudo chmod 755 /tmp/xfusioncorp.sh
```

#### 2-3. 무결성 검증 (Final Validation)

변경사항이 정확이 반영되었는지 재확인하여 실행 비트(x)가 활성화된 것을 검증함.

```bash
ls -l /tmp/xfusioncorp.sh
```

[출력 결과]

```bash
-rwxr-xr-x 1 root root 40 Jun 1 2026 /tmp/xfusioncorp.sh
```

### 3. 무엇을 배웠는가 (Takeaway)

#### 리눅스 권한(Permission)의 실무적 의미

- **8진수 모드(Octal Mode) vs 심볼릭 모드(Symbolic Mode)**: 실무에서는 `755`나 `644`처럼 8진수 모드를 더 선호. 이는 파일의 최종 상태를 명확하게 정의(Explicit)하여, 기존 권한에 의존하지 않고 확실한 보안 상태를 보장하기 때문.

- **755 권한의 선택 이유**: `755`는 소유자에게는 모든 권한을 주되, 그룹과 나머지 유저에게는 '수정' 권한을 제외한 '읽기 및 실행'만 허용하는 표준적인 실행 파일 권한이다. 이는 스크립트가 타인에 의해 변조되는 것을 막으면서도 자동화 도구들이 호출할 수 있게 만든다.

- **보안 주의사항**: `/tmp` 디렉토리 내의 파일에 과도한 권한을 주는 것을 경계해야 하며, 작업 완료 후에는 실제 서비스 계정으로 소유권을 이전하거나 적절한 위치로 이동시키는 후속 작업이 필요함.
